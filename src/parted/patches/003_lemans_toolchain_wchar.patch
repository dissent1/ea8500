diff -Nuar parted-1.8.7.orig/configure parted-1.8.7/configure
--- parted-1.8.7.orig/configure	2007-05-10 05:01:27.000000000 +0800
+++ parted-1.8.7/configure	2013-01-06 15:05:10.000000000 +0800
@@ -1,4 +1,5 @@
 #! /bin/sh
+# Modifications were made by Cisco Systems, Inc. on or before Wed Nov 30 13:28:05 PST 2012
 # Guess values for system-dependent variables and create Makefiles.
 # Generated by GNU Autoconf 2.61 for GNU parted 1.8.7.
 #
@@ -8757,6 +8758,8 @@
 #endif
 intmax_t i = INTMAX_MAX;
 uintmax_t j = UINTMAX_MAX;
+#undef WCHAR_MIN
+#define WCHAR_MIN	0
 struct s {
   int check_PTRDIFF: PTRDIFF_MIN < (ptrdiff_t) 0 && (ptrdiff_t) 0 < PTRDIFF_MAX ? 1 : -1;
   int check_SIG_ATOMIC: SIG_ATOMIC_MIN <= (sig_atomic_t) 0 && (sig_atomic_t) 0 < SIG_ATOMIC_MAX ? 1 : -1;
